# The CLARK Field Guide to ⟨Domain⟩ — v0.1 (draft)

- **Curator(s):** ⟨Name(s) — the byline; credit lives here and in the citation, never in folder names⟩
- **Scope:** ⟨2–3 sentences: what this domain covers, what it deliberately excludes⟩
- **Last verified:** ⟨YYYY-MM-DD — the date the venue/criteria sources were last checked⟩
- **Status:** draft → review → released (DOI minted on release)
- **Cite as:** ⟨Curator(s)⟩ (⟨Year⟩). *The CLARK Field Guide to ⟨Domain⟩* (v⟨X.Y⟩). Zenodo. ⟨DOI on release⟩

## How the skills use this guide

Point a project at this folder (or copy the relevant files in): `pre-submission-review` loads `venues.md` + `criteria.md` at Step 0; `literature-review`/`literature-radar` seed a new project from `conventions-starter.md`; `writing-polish` reads the register notes. No skill changes — the guide is data.

## Changelog

- v0.1 (⟨date⟩) — initial draft.
